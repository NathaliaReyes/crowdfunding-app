{{#each projects as |project| }}
<div>
  <div>
    <h2>{{project.name}}</h2>
    <h3>{{project.needed_funding}} needed!</h3>
    <h3>Created by </h3>
  </div>
  <div>
    <p>{{project.description}}</p>
  </div>
</div>
{{/each}}