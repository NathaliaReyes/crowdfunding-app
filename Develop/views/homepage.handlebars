{{#each projects as |project| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji project}}} <a href="/projects/{{id}}">{{project.name}}</a></h2>
    <h3>{{project.needed_funding}} needed!</h3>
    <h3>Created by </h3>
  </div>
  <div class="col-md-6">
    <p>{{project.description}}</p>
  </div>
</div>
{{/each}}